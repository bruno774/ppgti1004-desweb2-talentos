spring.application.name=talentos

# ============================================================
# Datasource: Negócio (primário) — banco Supabase de negócio
# Usa jdbc-url (não url): HikariCP mapeia jdbc-url -> jdbcUrl via relaxed binding.
# driver-class-name removido: HikariCP auto-detecta o driver pelo prefixo jdbc:postgresql://
# ============================================================
spring.datasource.negocio.jdbc-url=jdbc:postgresql://db.saawelwhkonzkbiyijcz.supabase.co:5432/postgres
spring.datasource.negocio.username=postgres
spring.datasource.negocio.password=*77S_8Dzh#dy$AT

# ============================================================
# Datasource: Auditoria — banco Supabase de auditoria
# ============================================================
spring.datasource.auditoria.jdbc-url=jdbc:postgresql://db.xvfaslxsewlvrevqohpv.supabase.co:5432/postgres
spring.datasource.auditoria.username=postgres
spring.datasource.auditoria.password=mtAzrax5dPYzZKDE

# ============================================================
# JPA / Hibernate (informativo — não aplicado às factories manuais,
# que definem as propriedades via setJpaPropertyMap() nas @Configuration)
# ============================================================
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ============================================================
# Implementação de serviço ativa (padrao | auditoria)
# ============================================================
app.servico.implementacao=padrao
